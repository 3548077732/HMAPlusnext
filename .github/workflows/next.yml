name: Apatch KPM Build (Kernel 5.15+)
on:
  workflow_dispatch:
    inputs:
      kernel_arch:
        description: "设备架构（arm64/arm）"
        required: true
        default: "arm64"
        type: choice
        options:
          - arm64
          - arm

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 安装最小化工具链
        run: |
          sudo apt update -y
          sudo apt install -y build-essential git flex bison bc libssl-dev libelf-dev dwarves clang llvm lld binutils
          sudo apt clean
          chmod -R 777 $(pwd)

      - name: 克隆谷歌官方内核（android13-5.15 分支）
        run: |
          mkdir -p ./kernel-src ./kernel-headers
          git clone --depth 1 -b android13-5.15 https://android.googlesource.com/kernel/common.git ./kernel-src
          [ ! -f "./kernel-src/Makefile" ] && { echo "❌ 内核源码克隆失败！"; exit 1; }
          
          # 复制基础头文件
          ARCH=${{ github.event.inputs.kernel_arch }}
          cp -r ./kernel-src/include ./kernel-headers/
          [ "$ARCH" = "arm64" ] && [ -d "./kernel-src/arch/arm64/include" ] && cp -r ./kernel-src/arch/arm64/include ./kernel-headers/arch-include
          [ "$ARCH" = "arm" ] && [ -d "./kernel-src/arch/arm/include" ] && cp -r ./kernel-src/arch/arm/include ./kernel-headers/arch-include
          
          chmod -R 777 ./kernel-src ./kernel-headers
          echo "✅ 内核源码准备成功！"

      - name: 修改内核 Makefile，剥离 VDSO 依赖
        run: |
          ARCH=${{ github.event.inputs.kernel_arch }}
          cd ./kernel-src
          
          # 删除 VDSO 相关依赖和 Makefile
          if [ "$ARCH" = "arm64" ]; then
            sed -i '/vdso_prepare/d' arch/arm64/Makefile
            rm -rf arch/arm64/kernel/vdso/Makefile
            echo -e "all:\nclean:" > arch/arm64/kernel/vdso/Makefile
          else
            sed -i '/vdso_prepare/d' arch/arm/Makefile
            rm -rf arch/arm/kernel/vdso/Makefile
            echo -e "all:\nclean:" > arch/arm/kernel/vdso/Makefile
          fi
          sed -i '/VDSO/d' Makefile
          sed -i '/vdso/d' Makefile
          
          echo "✅ VDSO 依赖剥离完成！"
          cd ..

      - name: 补充并复制 kpm.h 到内核默认头文件路径（关键修复）
        run: |
          # 1. 先在 kernel-headers 中补充 kpm.h
          mkdir -p ./kernel-headers/linux
          chmod 777 ./kernel-headers/linux
          cat > ./kernel-headers/linux/kpm.h << EOF
          #ifndef __KPM_H__
          #define __KPM_H__
          #include <linux/types.h>
          #include <linux/list.h>
          typedef void (*kpm_hook_func_t)(void *args, void *udata);
          struct kpm_hook {
              int syscall;
              kpm_hook_func_t func;
              void *udata;
              struct list_head list;
          };
          #define hook_fargs1_t struct { void *args[1]; }
          #define hook_fargs4_t struct { void *args[4]; }
          #define hook_fargs5_t struct { void *args[5]; }
          #define KPM_HOOK(syscall, func, udata) { \
              .syscall = syscall, \
              .func = (kpm_hook_func_t)func, \
              .udata = udata, \
              .list = LIST_HEAD_INIT((((struct kpm_hook *)0)->list)), \
          }
          int kpm_register_hooks(const struct kpm_hook *hooks, size_t num);
          void kpm_unregister_hooks(const struct kpm_hook *hooks, size_t num);
          #endif // __KPM_H__
          EOF
          
          # 2. 关键步骤：复制 kpm.h 到内核源码的 include/linux 目录（内核默认搜索路径）
          cp -f ./kernel-headers/linux/kpm.h ./kernel-src/include/linux/
          chmod 777 ./kernel-src/include/linux/kpm.h
          
          echo "✅ kpm.h 补充并复制到内核默认路径！"

      - name: 配置内核
        run: |
          ARCH=${{ github.event.inputs.kernel_arch }}
          cd ./kernel-src
          
          export CC=clang
          export LD=ld.lld
          export OBJCOPY=objcopy
          export NM=nm
          
          # 生成配置并启用模块支持
          make ARCH=$ARCH CC=$CC LD=$LD defconfig
          cat >> .config << EOF
          CONFIG_MODULES=y
          CONFIG_MODULE_UNLOAD=y
          CONFIG_MODULE_FORCE_LOAD=y
          CONFIG_ANDROID=y
          CONFIG_MODULE_SIG=n
          CONFIG_LLVM=y
          CONFIG_LLVM_IAS=y
          CONFIG_RETPOLINE=y
          CONFIG_STACKPROTECTOR=n
          CONFIG_DEBUG_INFO=n
          CONFIG_VDSO=n
          CONFIG_VDSO32=n
          CONFIG_COMPAT_VDSO=n
          CONFIG_ARM64_VDSO=n
          CONFIG_ARM64_COMPAT_VDSO=n
          EOF
          make ARCH=$ARCH CC=$CC LD=$LD oldconfig -j1 > /dev/null 2>&1
          
          # 准备模块编译环境和必需头文件
          make ARCH=$ARCH CC=$CC LD=$LD modules_prepare -j2 2>/dev/null
          make ARCH=$ARCH CC=$CC LD=$LD include/generated/asm-offsets.h -j2 2>/dev/null
          
          cd ..
          echo "✅ 内核配置完成！"

      - name: 编译 HMA++ KPM 模块（强化路径配置）
        run: |
          export KERNELDIR=$(pwd)/kernel-src
          export ARCH=${{ github.event.inputs.kernel_arch }}
          
          # 工具链配置+强化头文件路径
          export CC=clang
          export LD=ld.lld
          export OBJCOPY=objcopy
          export NM=nm
          
          if [ "$ARCH" = "arm64" ]; then
            # 明确包含 kernel-headers/linux 和内核自带的 include/linux 路径
            export EXTRA_CFLAGS="-I$(pwd)/kernel-headers -I$(pwd)/kernel-headers/linux -I$KERNELDIR/include -I$KERNELDIR/include/linux -I$KERNELDIR/arch/arm64/include/generated -Wall -Wextra -O2 -fPIC -w -DKERNEL_5_15 -fno-pie -nostdinc -DCONFIG_VDSO=n"
          else
            export EXTRA_CFLAGS="-I$(pwd)/kernel-headers -I$(pwd)/kernel-headers/linux -I$KERNELDIR/include -I$KERNELDIR/include/linux -I$KERNELDIR/arch/arm/include/generated -Wall -Wextra -O2 -fPIC -w -DKERNEL_5_15 -fno-pie -nostdinc -DCONFIG_VDSO=n"
          fi
          
          chmod 777 ./Makefile
          make clean || true
          # 编译模块
          make -j2 V=1 CC=$CC LD=$LD OBJCOPY=$OBJCOPY || { echo "❌ 编译失败！"; exit 1; }
          
          # 验证模块生成
          if [ -f "./HMA++.ko" ]; then
            echo "🎉 HMA++ KPM 模块编译成功！"
            ls -la ./HMA++.ko
            file ./HMA++.ko
          else
            echo "❌ 模块文件未生成！"
            exit 1
          fi
          chmod 777 ./HMA++.ko

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: HMA++-Next-KPM-${{ github.event.inputs.kernel_arch }}
          path: |
            HMA++.ko
            Makefile
          retention-days: 90
        if: success()
