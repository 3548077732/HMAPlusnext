name: Apatch KPM Build (Kernel 5.15+)
on:
  workflow_dispatch:
    inputs:
      kernel_arch:
        description: "设备架构（arm64/arm）"
        required: true
        default: "arm64"
        type: choice
        options:
          - arm64
          - arm

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 安装完整工具链（GCC+LLVM 双兼容）
        run: |
          sudo apt update -y
          # 同时安装 GCC（主机工具）和 LLVM（目标架构编译）
          sudo apt install -y build-essential git flex bison bc libssl-dev libelf-dev dwarves clang llvm lld binutils gcc
          sudo apt clean
          chmod -R 777 $(pwd)

      - name: 克隆谷歌官方内核（android13-5.15 分支）
        run: |
          mkdir -p ./kernel-src ./kernel-headers
          git clone --depth 1 -b android13-5.15 https://android.googlesource.com/kernel/common.git ./kernel-src
          [ ! -f "./kernel-src/Makefile" ] && { echo "❌ 内核源码克隆失败！"; exit 1; }
          
          # 全目录权限+复制头文件
          ARCH=${{ github.event.inputs.kernel_arch }}
          cp -r ./kernel-src/include ./kernel-headers/
          [ "$ARCH" = "arm64" ] && [ -d "./kernel-src/arch/arm64/include" ] && cp -r ./kernel-src/arch/arm64/include ./kernel-headers/arch-include
          [ "$ARCH" = "arm" ] && [ -d "./kernel-src/arch/arm/include" ] && cp -r ./kernel-src/arch/arm/include ./kernel-headers/arch-include
          chmod -R 777 ./kernel-src  # 递归开放所有权限，避免隐藏权限问题
          
          echo "✅ 内核源码准备成功！"

      - name: 修改内核 Makefile，剥离 VDSO+兼容配置
        run: |
          ARCH=${{ github.event.inputs.kernel_arch }}
          cd ./kernel-src
          
          # 1. 剥离 VDSO 依赖
          if [ "$ARCH" = "arm64" ]; then
            sed -i '/vdso_prepare/d' arch/arm64/Makefile
            rm -rf arch/arm64/kernel/vdso/Makefile
            echo -e "all:\nclean:" > arch/arm64/kernel/vdso/Makefile
          else
            sed -i '/vdso_prepare/d' arch/arm/Makefile
            rm -rf arch/arm/kernel/vdso/Makefile
            echo -e "all:\nclean:" > arch/arm/kernel/vdso/Makefile
          fi
          sed -i '/VDSO/d' Makefile
          sed -i '/vdso/d' Makefile
          
          # 2. 禁用不兼容参数
          sed -i 's/-mbranch-protection=pac-ret+leaf+bti//g' arch/$ARCH/Makefile
          sed -i 's/-Wa,-march//g' arch/$ARCH/Makefile
          
          chmod -R 777 ./arch ./Makefile
          echo "✅ VDSO 剥离+参数兼容完成！"
          cd ..

      - name: 补充 kpm.h 并双重复制+验证
        run: |
          # 生成 kpm.h
          cat > ./kpm.h << EOF
          #ifndef __KPM_H__
          #define __KPM_H__
          #include <linux/types.h>
          #include <linux/list.h>
          typedef void (*kpm_hook_func_t)(void *args, void *udata);
          struct kpm_hook {
              int syscall;
              kpm_hook_func_t func;
              void *udata;
              struct list_head list;
          };
          #define hook_fargs1_t struct { void *args[1]; }
          #define hook_fargs4_t struct { void *args[4]; }
          #define hook_fargs5_t struct { void *args[5]; }
          #define KPM_HOOK(syscall, func, udata) { \
              .syscall = syscall, \
              .func = (kpm_hook_func_t)func, \
              .udata = udata, \
              .list = LIST_HEAD_INIT((((struct kpm_hook *)0)->list)), \
          }
          int kpm_register_hooks(const struct kpm_hook *hooks, size_t num);
          void kpm_unregister_hooks(const struct kpm_hook *hooks, size_t num);
          #endif // __KPM_H__
          EOF
          
          # 双重复制+验证
          cp -f ./kpm.h ./kernel-src/include/
          cp -f ./kpm.h ./kernel-src/include/linux/
          [ ! -f "./kernel-src/include/kpm.h" ] && { echo "❌ kpm.h 复制失败！"; exit 1; }
          chmod 777 ./kernel-src/include/kpm.h ./kernel-src/include/linux/kpm.h
          
          echo "✅ kpm.h 准备完成！"

      - name: 配置内核（HOSTCC=gcc+极简配置）
        run: |
          ARCH=${{ github.event.inputs.kernel_arch }}
          cd ./kernel-src
          
          # 核心：目标架构用 LLVM，主机工具用 GCC（兼容 scripts/mod）
          export CC=clang
          export LD=ld.lld
          export OBJCOPY=objcopy
          export NM=nm
          export HOSTCC=gcc  # 显式指定主机工具链为 GCC
          export HOSTLD=gcc
          
          # 生成极简配置（仅保留模块必需功能，禁用所有非必要选项）
          make ARCH=$ARCH CC=$CC LD=$LD HOSTCC=$HOSTCC defconfig
          cat >> .config << EOF
          # 模块核心功能
          CONFIG_MODULES=y
          CONFIG_MODULE_UNLOAD=y
          CONFIG_MODULE_FORCE_LOAD=y
          CONFIG_MODULE_SIG=n
          CONFIG_MODULE_SIG_ALL=n
          CONFIG_MODULE_COMPRESS=n
          CONFIG_MODULE_ALLOW_MISSING_NAMESPACE_IMPORTS=y
          
          # 系统兼容
          CONFIG_ANDROID=y
          CONFIG_RETPOLINE=y
          
          # LLVM 兼容
          CONFIG_LLVM=y
          CONFIG_LLVM_IAS=y
          
          # 禁用所有不兼容/非必要功能
          CONFIG_STACKPROTECTOR=n
          CONFIG_DEBUG_INFO=n
          CONFIG_VDSO=n
          CONFIG_VDSO32=n
          CONFIG_COMPAT_VDSO=n
          CONFIG_ARM64_VDSO=n
          CONFIG_ARM64_COMPAT_VDSO=n
          CONFIG_ARM64_PTR_AUTH=n
          CONFIG_ARM64_BTI=n
          CONFIG_DEBUG_KERNEL=n
          CONFIG_DEBUG_MISC=n
          CONFIG_PROFILE=n
          CONFIG_TRACING=n
          CONFIG_KASAN=n
          CONFIG_KCSAN=n
          EOF
          # 应用配置（单线程，避免冲突）
          make ARCH=$ARCH CC=$CC LD=$LD HOSTCC=$HOSTCC oldconfig -j1 > /dev/null 2>&1
          
          # 准备模块环境（显式指定 HOSTCC）
          make ARCH=$ARCH CC=$CC LD=$LD HOSTCC=$HOSTCC modules_prepare -j1 2>/dev/null
          make ARCH=$ARCH CC=$CC LD=$LD HOSTCC=$HOSTCC include/generated/asm-offsets.h -j1 2>/dev/null
          
          chmod -R 777 ./include ./scripts  # 确保 scripts 目录可写
          cd ..
          echo "✅ 内核配置完成！HOST 工具链用 GCC 兼容！"

      - name: 编译 HMA++ KPM 模块（单线程+完整参数）
        run: |
          export KERNELDIR=$(pwd)/kernel-src
          export ARCH=${{ github.event.inputs.kernel_arch }}
          
          # 工具链配置（目标 LLVM + 主机 GCC）
          export CC=clang
          export LD=ld.lld
          export OBJCOPY=objcopy
          export NM=nm
          export HOSTCC=gcc
          
          # 目标架构参数（极简+兼容）
          if [ "$ARCH" = "arm64" ]; then
            export EXTRA_CFLAGS="-I$KERNELDIR/include -I$KERNELDIR/include/linux -I$(pwd)/kernel-headers -I$KERNELDIR/arch/arm64/include/generated -Wall -O2 -fPIC -w -DKERNEL_5_15 -fno-pie -nostdinc -march=armv8-a -mgeneral-regs-only"
          else
            export EXTRA_CFLAGS="-I$KERNELDIR/include -I$KERNELDIR/include/linux -I$(pwd)/kernel-headers -I$KERNELDIR/arch/arm/include/generated -Wall -O2 -fPIC -w -DKERNEL_5_15 -fno-pie -nostdinc -march=armv7-a -mgeneral-regs-only"
          fi
          
          chmod 777 ./Makefile
          make clean || true
          # 单线程编译（避免并发冲突）+ 显式传递所有参数
          make -j1 V=1 CC=$CC LD=$LD OBJCOPY=$OBJCOPY NM=$NM HOSTCC=$HOSTCC EXTRA_CFLAGS="$EXTRA_CFLAGS" || { echo "❌ 编译失败！"; exit 1; }
          
          # 验证模块生成
          if [ -f "./HMA++.ko" ]; then
            echo "🎉 HMA++ KPM 模块编译成功！"
            ls -la ./HMA++.ko
            file ./HMA++.ko
          else
            echo "❌ 模块文件未生成！"
            exit 1
          fi
          chmod 777 ./HMA++.ko

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: HMA++-Next-KPM-${{ github.event.inputs.kernel_arch }}
          path: |
            HMA++.ko
            Makefile
          retention-days: 90
        if: success()
