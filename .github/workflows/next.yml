name: Apatch KPM Build (Kernel 5.15+)
on:
  workflow_dispatch:
    inputs:
      kernel_arch:
        description: "设备架构（arm64/arm）"
        required: true
        default: "arm64"
        type: choice
        options:
          - arm64
          - arm

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 安装兼容工具链（GNU+LLVM 双工具链，解决格式兼容）
        run: |
          sudo apt update -y
          # 安装 GNU 工具链（objcopy 兼容 VDSO 格式）+ LLVM 工具链（编译内核）
          sudo apt install -y build-essential git flex bison bc libssl-dev libelf-dev dwarves clang llvm lld binutils
          sudo apt clean
          chmod -R 777 $(pwd)
          
          # 验证工具完整性（GNU objcopy + LLVM 核心工具）
          [ ! -f "$(which clang)" ] && { echo "❌ clang 缺失！"; exit 1; }
          [ ! -f "$(which ld.lld)" ] && { echo "❌ ld.lld 缺失！"; exit 1; }
          [ ! -f "$(which objcopy)" ] && { echo "❌ GNU objcopy 缺失！"; exit 1; }
          echo "✅ 双工具链安装成功！"

      - name: 克隆谷歌官方内核（android13-5.15 分支）
        run: |
          mkdir -p ./kernel-src ./kernel-headers
          git clone --depth 1 -b android13-5.15 https://android.googlesource.com/kernel/common.git ./kernel-src
          [ ! -f "./kernel-src/Makefile" ] && { echo "❌ 内核源码克隆失败！"; exit 1; }
          
          # 复制基础头文件
          ARCH=${{ github.event.inputs.kernel_arch }}
          cp -r ./kernel-src/include ./kernel-headers/
          [ "$ARCH" = "arm64" ] && [ -d "./kernel-src/arch/arm64/include" ] && cp -r ./kernel-src/arch/arm64/include ./kernel-headers/arch-include
          [ "$ARCH" = "arm" ] && [ -d "./kernel-src/arch/arm/include" ] && cp -r ./kernel-src/arch/arm/include ./kernel-headers/arch-include
          
          chmod -R 777 ./kernel-src ./kernel-headers
          echo "✅ 内核源码准备成功！"

      - name: 配置内核（彻底阻断 VDSO 编译+工具链兼容）
        run: |
          ARCH=${{ github.event.inputs.kernel_arch }}
          cd ./kernel-src
          
          # 工具链配置：LLVM 编译 + GNU objcopy（兼容 VDSO 格式）
          export CC=clang
          export LD=ld.lld
          export OBJCOPY=objcopy  # 改用 GNU objcopy，放弃 llvm-objcopy
          export NM=nm
          export OBJDUMP=objdump
          export STRIP=strip
          
          # 1. 生成默认配置
          make ARCH=$ARCH CC=$CC LD=$LD defconfig
          
          # 2. 终极禁用 VDSO（覆盖所有相关配置，无遗漏）
          cat >> .config << EOF
          # 彻底禁用 VDSO 及所有相关功能
          CONFIG_VDSO=n
          CONFIG_VDSO32=n
          CONFIG_COMPAT_VDSO=n
          CONFIG_VDSO_COMPAT=n
          CONFIG_VDSO_PRELINK=n
          CONFIG_ARM64_VDSO=n
          CONFIG_ARM64_COMPAT_VDSO=n
          # 禁用 VDSO 依赖的调试功能
          CONFIG_DEBUG_INFO=n
          CONFIG_DEBUG_INFO_BTF=n
          EOF
          
          # 3. 应用配置并生成最终 .config（确保配置生效）
          make ARCH=$ARCH CC=$CC LD=$LD oldconfig -j1
          
          # 4. 启用模块支持+LLVM 兼容配置
          sed -i 's/# CONFIG_MODULES is not set/CONFIG_MODULES=y/' .config
          sed -i 's/# CONFIG_MODULE_UNLOAD is not set/CONFIG_MODULE_UNLOAD=y/' .config
          sed -i 's/# CONFIG_MODULE_FORCE_LOAD is not set/CONFIG_MODULE_FORCE_LOAD=y/' .config
          sed -i 's/# CONFIG_RETPOLINE is not set/CONFIG_RETPOLINE=y/' .config
          sed -i 's/# CONFIG_ANDROID is not set/CONFIG_ANDROID=y/' .config
          sed -i 's/# CONFIG_MODULE_SIG is not set/CONFIG_MODULE_SIG=n/' .config
          sed -i 's/# CONFIG_LLVM=y/CONFIG_LLVM=y/' .config
          sed -i 's/# CONFIG_LLVM_IAS=y/CONFIG_LLVM_IAS=y/' .config
          sed -i 's/CONFIG_STACKPROTECTOR=y/# CONFIG_STACKPROTECTOR is not set/' .config
          
          # 5. 编译准备（显式跳过 VDSO 目标，阻断依赖）
          make ARCH=$ARCH CC=$CC LD=$LD prepare modules_prepare -j2 \
            VDSO_PREPARE= \
            arch/arm64/kernel/vdso/= \
            2>/dev/null  # 忽略 VDSO 相关警告
          
          cd ..
          echo "✅ 内核配置完整！VDSO 已彻底阻断，工具链兼容！"

      - name: 补充 KPM 核心头文件
        run: |
          mkdir -p ./kernel-headers/linux
          chmod 777 ./kernel-headers/linux
          cat > ./kernel-headers/linux/kpm.h << EOF
          #ifndef __KPM_H__
          #define __KPM_H__
          #include <linux/types.h>
          #include <linux/list.h>
          typedef void (*kpm_hook_func_t)(void *args, void *udata);
          struct kpm_hook {
              int syscall;
              kpm_hook_func_t func;
              void *udata;
              struct list_head list;
          };
          #define hook_fargs1_t struct { void *args[1]; }
          #define hook_fargs4_t struct { void *args[4]; }
          #define hook_fargs5_t struct { void *args[5]; }
          #define KPM_HOOK(syscall, func, udata) { \
              .syscall = syscall, \
              .func = (kpm_hook_func_t)func, \
              .udata = udata, \
              .list = LIST_HEAD_INIT((((struct kpm_hook *)0)->list)), \
          }
          int kpm_register_hooks(const struct kpm_hook *hooks, size_t num);
          void kpm_unregister_hooks(const struct kpm_hook *hooks, size_t num);
          #endif // __KPM_H__
          EOF
          chmod 777 ./kernel-headers/linux/kpm.h

      - name: 编译 HMA++ KPM 模块（最终构建）
        run: |
          export KERNELDIR=$(pwd)/kernel-src
          export ARCH=${{ github.event.inputs.kernel_arch }}
          
          # 工具链配置（保持与内核配置一致）
          export CC=clang
          export LD=ld.lld
          export OBJCOPY=objcopy
          export NM=nm
          export OBJDUMP=objdump
          
          if [ "$ARCH" = "arm64" ]; then
            export EXTRA_CFLAGS="-I$(pwd)/kernel-headers -I$(pwd)/kernel-headers/linux -I$(pwd)/kernel-headers/arch-include -I$KERNELDIR/arch/arm64/include/generated -Wall -Wextra -O2 -fPIC -w -DKERNEL_5_15 -fno-pie -nostdinc"
          else
            export EXTRA_CFLAGS="-I$(pwd)/kernel-headers -I$(pwd)/kernel-headers/linux -I$(pwd)/kernel-headers/arch-include -I$KERNELDIR/arch/arm/include/generated -Wall -Wextra -O2 -fPIC -w -DKERNEL_5_15 -fno-pie -nostdinc"
          fi
          
          chmod 777 ./Makefile
          make clean || true
          # 编译模块（显式传递工具链参数，阻断 VDSO 依赖）
          make -j2 V=1 CC=$CC LD=$LD OBJCOPY=$OBJCOPY \
            EXTRA_CFLAGS="$EXTRA_CFLAGS -DCONFIG_VDSO=n" || { echo "❌ 编译失败！"; exit 1; }
          
          # 验证模块生成
          if [ -f "./HMA++.ko" ]; then
            echo "🎉 HMA++ KPM 模块编译成功！"
            ls -la ./HMA++.ko
            file ./HMA++.ko  # 确认模块架构和格式正确
          else
            echo "❌ 模块文件未生成！"
            exit 1
          fi
          chmod 777 ./HMA++.ko

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: HMA++-Next-KPM-${{ github.event.inputs.kernel_arch }}
          path: |
            HMA++.ko
            Makefile
          retention-days: 90
        if: success()
