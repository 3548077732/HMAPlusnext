name: Apatch KPM Build (Kernel 5.15+)
on:
  workflow_dispatch:
    inputs:
      kernel_arch:
        description: "设备架构（arm64/arm）"
        required: true
        default: "arm64"
        type: choice
        options:
          - arm64
          - arm

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 安装完整工具链（GCC+LLVM+依赖补全）
        run: |
          sudo apt update -y
          # 补全可能缺失的依赖，确保 scripts/mod 编译成功
          sudo apt install -y build-essential git flex bison bc libssl-dev libelf-dev dwarves clang llvm lld binutils gcc libc6-dev
          sudo apt clean
          chmod -R 777 $(pwd)

      - name: 克隆谷歌官方内核（android13-5.15 分支）
        run: |
          mkdir -p ./kernel-src ./kernel-headers
          git clone --depth 1 -b android13-5.15 https://android.googlesource.com/kernel/common.git ./kernel-src
          [ ! -f "./kernel-src/Makefile" ] && { echo "❌ 内核源码克隆失败！"; exit 1; }
          
          # 全目录递归权限+头文件复制
          ARCH=${{ github.event.inputs.kernel_arch }}
          cp -r ./kernel-src/include ./kernel-headers/
          [ "$ARCH" = "arm64" ] && [ -d "./kernel-src/arch/arm64/include" ] && cp -r ./kernel-src/arch/arm64/include ./kernel-headers/arch-include
          [ "$ARCH" = "arm" ] && [ -d "./kernel-src/arch/arm/include" ] && cp -r ./kernel-src/arch/arm/include ./kernel-headers/arch-include
          chmod -R 777 ./kernel-src  # 确保所有文件可读写执行
          
          echo "✅ 内核源码准备成功！"

      - name: 修改内核 Makefile（剥离 VDSO+scripts/mod 兼容）
        run: |
          ARCH=${{ github.event.inputs.kernel_arch }}
          cd ./kernel-src
          
          # 1. 剥离 VDSO 依赖
          if [ "$ARCH" = "arm64" ]; then
            sed -i '/vdso_prepare/d' arch/arm64/Makefile
            rm -rf arch/arm64/kernel/vdso/Makefile
            echo -e "all:\nclean:" > arch/arm64/kernel/vdso/Makefile
          else
            sed -i '/vdso_prepare/d' arch/arm/Makefile
            rm -rf arch/arm/kernel/vdso/Makefile
            echo -e "all:\nclean:" > arch/arm/kernel/vdso/Makefile
          fi
          sed -i '/VDSO/d' Makefile
          sed -i '/vdso/d' Makefile
          
          # 2. 禁用不兼容参数
          sed -i 's/-mbranch-protection=pac-ret+leaf+bti//g' arch/$ARCH/Makefile
          sed -i 's/-Wa,-march//g' arch/$ARCH/Makefile
          
          # 3. 修复 scripts/mod/Makefile：添加 clang 兼容参数
          sed -i '/CCFLAGS/ s/$/ -mgeneral-regs-only -fno-asynchronous-unwind-tables -fno-unwind-tables/' scripts/mod/Makefile
          
          chmod -R 777 ./arch ./Makefile ./scripts
          echo "✅ Makefile 修复完成！"
          cd ..

      - name: 补充 kpm.h 并双重复制+验证
        run: |
          # 生成 kpm.h
          cat > ./kpm.h << EOF
          #ifndef __KPM_H__
          #define __KPM_H__
          #include <linux/types.h>
          #include <linux/list.h>
          typedef void (*kpm_hook_func_t)(void *args, void *udata);
          struct kpm_hook {
              int syscall;
              kpm_hook_func_t func;
              void *udata;
              struct list_head list;
          };
          #define hook_fargs1_t struct { void *args[1]; }
          #define hook_fargs4_t struct { void *args[4]; }
          #define hook_fargs5_t struct { void *args[5]; }
          #define KPM_HOOK(syscall, func, udata) { \
              .syscall = syscall, \
              .func = (kpm_hook_func_t)func, \
              .udata = udata, \
              .list = LIST_HEAD_INIT((((struct kpm_hook *)0)->list)), \
          }
          int kpm_register_hooks(const struct kpm_hook *hooks, size_t num);
          void kpm_unregister_hooks(const struct kpm_hook *hooks, size_t num);
          #endif // __KPM_H__
          EOF
          
          # 双重复制+验证
          cp -f ./kpm.h ./kernel-src/include/
          cp -f ./kpm.h ./kernel-src/include/linux/
          [ ! -f "./kernel-src/include/kpm.h" ] && { echo "❌ kpm.h 复制失败！"; exit 1; }
          chmod 777 ./kernel-src/include/kpm.h ./kernel-src/include/linux/kpm.h
          
          echo "✅ kpm.h 准备完成！"

      - name: 配置内核（极简+强制禁用模块干扰功能）
        run: |
          ARCH=${{ github.event.inputs.kernel_arch }}
          cd ./kernel-src
          
          # 工具链配置（目标 LLVM + 主机 GCC）
          export CC=clang
          export LD=ld.lld
          export OBJCOPY=objcopy
          export NM=nm
          export HOSTCC=gcc
          export HOSTLD=gcc
          
          # 生成极简配置（彻底禁用模块相关干扰功能）
          make ARCH=$ARCH CC=$CC LD=$LD HOSTCC=$HOSTCC defconfig
          cat >> .config << EOF
          # 模块核心功能（仅保留必需）
          CONFIG_MODULES=y
          CONFIG_MODULE_UNLOAD=y
          CONFIG_MODULE_FORCE_LOAD=y
          CONFIG_MODULE_SIG=n
          CONFIG_MODULE_SIG_ALL=n
          CONFIG_MODULE_SIG_FORCE=n
          CONFIG_MODULE_SIG_HASH=n
          CONFIG_MODULE_COMPRESS=n
          CONFIG_MODULE_DECOMPRESS=n
          CONFIG_MODULE_ALLOW_MISSING_NAMESPACE_IMPORTS=y
          CONFIG_MODULES_TREE_LOOKUP=n
          
          # 系统兼容
          CONFIG_ANDROID=y
          CONFIG_RETPOLINE=y
          CONFIG_MMU=y  # 确保 MMU 启用，避免模块编译依赖缺失
          
          # LLVM 兼容
          CONFIG_LLVM=y
          CONFIG_LLVM_IAS=y
          
          # 禁用所有非必要/不兼容功能
          CONFIG_STACKPROTECTOR=n
          CONFIG_DEBUG_INFO=n
          CONFIG_VDSO=n
          CONFIG_VDSO32=n
          CONFIG_COMPAT_VDSO=n
          CONFIG_ARM64_VDSO=n
          CONFIG_ARM64_COMPAT_VDSO=n
          CONFIG_ARM64_PTR_AUTH=n
          CONFIG_ARM64_BTI=n
          CONFIG_DEBUG_KERNEL=n
          CONFIG_DEBUG_MISC=n
          CONFIG_PROFILE=n
          CONFIG_TRACING=n
          CONFIG_KASAN=n
          CONFIG_KCSAN=n
          CONFIG_SECURITY=n  # 禁用安全模块，避免干扰
          CONFIG_SIGNED_MODULES=n
          EOF
          # 应用配置（单线程，避免冲突）
          make ARCH=$ARCH CC=$CC LD=$LD HOSTCC=$HOSTCC oldconfig -j1 > /dev/null 2>&1
          
          echo "✅ 内核配置完成！"
          cd ..

      - name: 手动编译 scripts/mod（解决 empty.o 失败）
        run: |
          ARCH=${{ github.event.inputs.kernel_arch }}
          cd ./kernel-src
          
          # 工具链配置
          export CC=clang
          export LD=ld.lld
          export OBJCOPY=objcopy
          export NM=nm
          export HOSTCC=gcc
          
          # 手动编译 scripts/mod 目录，添加强制兼容参数
          make ARCH=$ARCH CC=$CC LD=$LD HOSTCC=$HOSTCC -C scripts/mod \
            CFLAGS="-mgeneral-regs-only -fno-asynchronous-unwind-tables -fno-unwind-tables -w -O2" \
            -j1 2>/dev/null || { echo "❌ scripts/mod 编译失败！"; exit 1; }
          
          # 继续完成模块准备
          make ARCH=$ARCH CC=$CC LD=$LD HOSTCC=$HOSTCC modules_prepare -j1 2>/dev/null
          make ARCH=$ARCH CC=$CC LD=$LD HOSTCC=$HOSTCC include/generated/asm-offsets.h -j1 2>/dev/null
          
          chmod -R 777 ./scripts/mod ./include
          echo "✅ scripts/mod 编译成功！模块环境准备完成！"
          cd ..

      - name: 编译 HMA++ KPM 模块（最终构建）
        run: |
          export KERNELDIR=$(pwd)/kernel-src
          export ARCH=${{ github.event.inputs.kernel_arch }}
          
          # 工具链配置
          export CC=clang
          export LD=ld.lld
          export OBJCOPY=objcopy
          export NM=nm
          export HOSTCC=gcc
          
          # 目标架构兼容参数（极简+强制）
          if [ "$ARCH" = "arm64" ]; then
            export EXTRA_CFLAGS="-I$KERNELDIR/include -I$KERNELDIR/include/linux -I$(pwd)/kernel-headers -I$KERNELDIR/arch/arm64/include/generated -Wall -O2 -fPIC -w -DKERNEL_5_15 -fno-pie -nostdinc -march=armv8-a -mgeneral-regs-only -fno-asynchronous-unwind-tables -fno-unwind-tables -mno-unaligned-access"
          else
            export EXTRA_CFLAGS="-I$KERNELDIR/include -I$KERNELDIR/include/linux -I$(pwd)/kernel-headers -I$KERNELDIR/arch/arm/include/generated -Wall -O2 -fPIC -w -DKERNEL_5_15 -fno-pie -nostdinc -march=armv7-a -mgeneral-regs-only -fno-asynchronous-unwind-tables -fno-unwind-tables -mno-unaligned-access"
          fi
          
          chmod 777 ./Makefile
          make clean || true
          # 单线程编译，显式传递所有参数
          make -j1 V=1 CC=$CC LD=$LD OBJCOPY=$OBJCOPY NM=$NM HOSTCC=$HOSTCC EXTRA_CFLAGS="$EXTRA_CFLAGS" || { echo "❌ 模块编译失败！"; exit 1; }
          
          # 验证模块生成
          if [ -f "./HMA++.ko" ]; then
            echo "🎉 HMA++ KPM 模块编译成功！"
            ls -la ./HMA++.ko
            file ./HMA++.ko
          else
            echo "❌ 模块文件未生成！"
            exit 1
          fi
          chmod 777 ./HMA++.ko

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: HMA++-Next-KPM-${{ github.event.inputs.kernel_arch }}
          path: |
            HMA++.ko
            Makefile
          retention-days: 90
        if: success()
